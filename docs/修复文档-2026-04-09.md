# 修复文档（持续更新）

> 更新时间：2026-04-09  
> 说明：按需求序号逐项登记，状态含 `已完成` / `进行中` / `待确认`

## 本轮修复进度

1. 后台 AI 变现工具分页 + 后台缺分页检查  
   - 状态：**已完成（第一批）**  
   - 已补分页：`AI工具变现`、`会员资源`、`私域SOP`、`副业案例`（前后端均支持 `page/per_page` + 搜索）。

2. `scripts/server-update.sh` 仅保留拉代码/迁移/清缓存  
   - 状态：**已完成**  
   - 已移除：`composer install`、`npm build`、`restart` 等步骤。

3. 站内通知未推送给用户  
   - 状态：**已完成（支持按人群 + 补派发兜底）**  
   - 已加：`system_notifications.audience`（all/non_member/member/vip/svip/admin）；派发按人群过滤用户；后台通知列表读取时自动补派发历史已发布未派发数据（`dispatchBacklog`）。

4. AI 采集 `summary` 超长报错 + 跳过原因  
   - 状态：**已完成（第一批）**  
   - 已修：入库前按字段长度截断（如 `summary` 500）；重复跳过会返回明确原因。

5. 广告位前台不展示（所有人可见、浮动）  
   - 状态：**已完成（第一批）**  
   - 已修：浮动广告解析支持 `type=float` 与左右位兜底转浮动展示。

6. VIP 皮肤不切换 / SVIP 图标不展示  
   - 状态：**已完成（第一批）**  
   - 已修：前端皮肤组件按 `user-role` 做 `vip/svip/admin` 权限；未登录跳登录；新增 `SVIP` 徽标。

7. 后台邮件记录为空（实际有发信）  
   - 状态：**进行中（已补主链路日志）**  
   - 已补：注册验证码、欢迎信、邮箱变更验证码、后台测试发信、每日摘要、会员到期提醒 均写入 `email_logs`。

8. 前台邮件订阅功能（仅 VIP/SVIP）  
   - 状态：**已完成（第一批）**  
   - 已加：个人中心订阅表单（VIP/SVIP/Admin 可见）；后端接口增加角色校验。

9. 实时动态与 VIP 专属宽度对齐  
   - 状态：**已完成**  
   - 已将实时动态容器由 `max-w-4xl` 调整到 `max-w-7xl`。

10. 前端公共权限遮罩组件  
    - 状态：**已完成（第一批）**  
    - 新增：`partials/access-mask.blade.php`，用于权限不足时覆盖并跳转开通页。

11. VIP 权益页展示当前权益/到期时间/角色权限  
    - 状态：**已完成（第一批）**  
    - 已加：当前身份、到期时间；`SVIP/Admin` 不再显示 `VIP` 付费入口。

12. 个人中心展示到期时间 + 三天倒计时高亮  
    - 状态：**已完成**  
    - 已加：<=3 天红色提示 + 秒级倒计时。

13. 案例页评论功能  
    - 状态：**已完成（第一批）**  
    - 已加：案例评论控制器、路由、详情页评论区、点赞/回复/举报。

14. 点击项目详情报错  
    - 状态：**已完成**  
    - 已修：`tags` 字符串与数组兼容，避免 `count(string)` 报错。

15. 个人中心“我的订单”+ 用户评价（订单页评论）  
    - 状态：**已完成（评价进入首页评价池）**  
    - 已加：个人中心“我的订单”页；已支付订单可提交评价；评价写入 `site_testimonials` 且默认不发布，后台“首页评价”审核发布后展示到首页。

16. 首页底部评价 + 新增精品文章/项目/案例滚动  
    - 状态：**已完成（第一批）**  
    - 已加：`精品内容`三组滚动卡片（文章/项目/案例，按热度排序）。

17. 点赞收藏改成 AJAX 无刷新  
    - 状态：**已完成（第一批）**  
    - 已加：通用 `engagement-scripts` + 文章/项目/投稿三处按钮无刷新更新。

18. 每完成一项更新修复文档  
    - 状态：**进行中（当前文档已创建）**

## 待确认

- 站内通知的 `audience=user`（仅登录用户）是否需要单独支持？目前站内信天然是用户维度，`all` 等价“所有用户”。

---

## 2026-04-10 第二批修复（本次对话）

1. 点赞/收藏报错  
   - 状态：**已完成**  
   - 修复：`ArticleEngagementController`、`ProjectEngagementController`、`UserPostEngagementController` 的事务闭包补齐 `use ($request)`，修复 `Undefined variable $request`。

2. 个人中心无到期信息 + 布局不合理  
   - 状态：**已完成（第一版）**  
   - 修复：个人中心改为左侧菜单、右侧内容布局；增加 `vipExpiresAt` 展示逻辑，会员到期时间在个人资料区可见。

3. 邮件订阅能力不足（内容 + 每个内容发送时间）  
   - 状态：**已完成（第一版）**  
   - 修复：`email_subscriptions` 新增 `topic_schedule`（JSON）；前台订阅支持选择多个主题并按主题保存 `HH:mm` 发送时间；后台 API 同步支持该字段。

4. 案例详情左右留白过大  
   - 状态：**已完成（第一版）**  
   - 修复：案例详情页改为 `max-w-7xl` + 双栏布局，右侧新增“相关推荐”。

5. 后台成功操作缺少弹窗反馈  
   - 状态：**已完成（第一版）**  
   - 修复：`resources/js/admin/main.js` 增加 Axios 全局响应拦截，POST/PUT/PATCH/DELETE 返回 `message` 时统一成功提示，异常统一错误提示。

6. 广告位前台类型/位置不生效 + 字体过小  
   - 状态：**已完成（第一版）**  
   - 修复：浮动广告渲染按 `position`（left/right/top/bottom）计算定位，按 `type` 调整默认尺寸；标题字号由 `text-[10px]` 调整到 `text-xs`。

7. 后台分页仍有漏网页面  
   - 状态：**进行中（本次新增一页）**  
   - 已补：`AdSlots` 改为 `page/per_page/q` + `AdminPagination` 组件。

8. 皮肤权限与切换异常（VIP/SVIP）  
   - 状态：**已完成（第一版）**  
   - 修复：`SkinController` 默认皮肤从 `type=default` 改为 `type=free`；补齐 `svip` 权限校验，避免会员切换闪回。

9. 权限遮罩视觉不符合预期  
   - 状态：**已完成（第一版）**  
   - 修复：增强遮罩为高不透明度覆盖（保留少量内容可见）；新增公共组件 `vip-mask` / `svip-mask`。

10. 后台“难用”界面优化（不改功能，统一框架风格）  
   - 状态：**进行中（已迁移 3 页）**  
   - 说明：后台技术栈为 Vue3 + Element Plus；已新增通用页面壳组件 `AdminPageShell`、通用卡片容器 `AdminCard`，并开始迁移高频列表页。
   - 已迁移：
     - `订单管理`：支持搜索（订单号/用户）、每页条数、统一分页组件
     - `邮件发送记录`：支持搜索（收件人/主题/模板键）、每页条数、统一分页组件
     - `系统通知`：支持搜索（标题/内容）、每页条数、统一分页组件

11. 后台菜单权限补齐（前端菜单 + 后端接口一致）  
   - 状态：**已完成（第一版）**  
   - 修复：新增 `perm` 权限中间件；`routes/admin_api.php` 按模块为后台接口挂载 `admin:*:read` 权限点，避免“菜单隐藏/接口仍可访问”或“菜单可见/接口 403”不一致。

## 2026-04-10 第二批修复（续）

12. SVIP 皮肤定制（月限 1 次，仅本人可用）  
   - 状态：**已完成（第一版）**  
   - 修复：新增 `skin_configs` 私有字段（`owner_user_id/is_private/custom_source`）；前台 `SVIP 定制订阅`页新增“皮肤定制”表单；后端按“自然月 1 次 + SVIP 有效期”校验，创建后自动启用，且仅创建者本人可见/可切换。

13. 前后台 session 不共享（域名拆分场景）  
   - 状态：**已完成（第一版）**  
   - 修复：`config/session.php` 增加域名自动推导（基于 `APP_FRONT_DOMAIN/ADMIN_DOMAIN`）；`same_site` 改为环境可配置；`config/sanctum.php` 自动合并前后台域名到 stateful 列表，降低跨子域登录态丢失问题。

14. 后台用户管理可配置能力不足（基于 users/admin_users）  
   - 状态：**已完成（第一版）**  
   - 修复：`UserController` 增强筛选（角色/禁用状态）；新增后台账号档案接口（`display_name/is_active/is_super/角色组`）与重置密码接口；`UserEdit` 增加“后台账号档案（admin_users）”和“安全与登录”操作区。

15. 后台列表页统一框架交互（持续迁移）  
   - 状态：**进行中（本次再迁移 2 页）**  
   - 已迁移：`Users.vue`、`SvipCustomSubscriptionsIndex.vue` 统一为 `AdminPageShell + AdminCard + AdminPagination`，补齐每页条数与筛选参数透传。

16. 首页精品文章/项目/案例样式与权限遮罩优化  
   - 状态：**已完成（第一版）**  
   - 修复：重做精品卡片视觉（层次、阴影、信息结构）；案例滚动区支持 `vip` 可见性识别，普通用户显示 `vip-mask` 遮罩并引导至开通页。
