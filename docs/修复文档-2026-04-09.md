# 修复文档（持续更新）

> 更新时间：2026-04-09  
> 说明：按需求序号逐项登记，状态含 `已完成` / `进行中` / `待确认`

## 本轮修复进度

1. 后台 AI 变现工具分页 + 后台缺分页检查  
   - 状态：**已完成（第一批）**  
   - 已补分页：`AI工具变现`、`会员资源`、`私域SOP`、`副业案例`（前后端均支持 `page/per_page` + 搜索）。

2. `scripts/server-update.sh` 仅保留拉代码/迁移/清缓存  
   - 状态：**已完成**  
   - 已移除：`composer install`、`npm build`、`restart` 等步骤。

3. 站内通知未推送给用户  
   - 状态：**已完成（支持按人群 + 补派发兜底）**  
   - 已加：`system_notifications.audience`（all/non_member/member/vip/svip/admin）；派发按人群过滤用户；后台通知列表读取时自动补派发历史已发布未派发数据（`dispatchBacklog`）。

4. AI 采集 `summary` 超长报错 + 跳过原因  
   - 状态：**已完成（第一批）**  
   - 已修：入库前按字段长度截断（如 `summary` 500）；重复跳过会返回明确原因。

5. 广告位前台不展示（所有人可见、浮动）  
   - 状态：**已完成（第一批）**  
   - 已修：浮动广告解析支持 `type=float` 与左右位兜底转浮动展示。

6. VIP 皮肤不切换 / SVIP 图标不展示  
   - 状态：**已完成（第一批）**  
   - 已修：前端皮肤组件按 `user-role` 做 `vip/svip/admin` 权限；未登录跳登录；新增 `SVIP` 徽标。

7. 后台邮件记录为空（实际有发信）  
   - 状态：**进行中（已补主链路日志）**  
   - 已补：注册验证码、欢迎信、邮箱变更验证码、后台测试发信、每日摘要、会员到期提醒 均写入 `email_logs`。

8. 前台邮件订阅功能（仅 VIP/SVIP）  
   - 状态：**已完成（第一批）**  
   - 已加：个人中心订阅表单（VIP/SVIP/Admin 可见）；后端接口增加角色校验。

9. 实时动态与 VIP 专属宽度对齐  
   - 状态：**已完成**  
   - 已将实时动态容器由 `max-w-4xl` 调整到 `max-w-7xl`。

10. 前端公共权限遮罩组件  
    - 状态：**已完成（第一批）**  
    - 新增：`partials/access-mask.blade.php`，用于权限不足时覆盖并跳转开通页。

11. VIP 权益页展示当前权益/到期时间/角色权限  
    - 状态：**已完成（第一批）**  
    - 已加：当前身份、到期时间；`SVIP/Admin` 不再显示 `VIP` 付费入口。

12. 个人中心展示到期时间 + 三天倒计时高亮  
    - 状态：**已完成**  
    - 已加：<=3 天红色提示 + 秒级倒计时。

13. 案例页评论功能  
    - 状态：**已完成（第一批）**  
    - 已加：案例评论控制器、路由、详情页评论区、点赞/回复/举报。

14. 点击项目详情报错  
    - 状态：**已完成**  
    - 已修：`tags` 字符串与数组兼容，避免 `count(string)` 报错。

15. 个人中心“我的订单”+ 用户评价（订单页评论）  
    - 状态：**已完成（评价进入首页评价池）**  
    - 已加：个人中心“我的订单”页；已支付订单可提交评价；评价写入 `site_testimonials` 且默认不发布，后台“首页评价”审核发布后展示到首页。

16. 首页底部评价 + 新增精品文章/项目/案例滚动  
    - 状态：**已完成（第一批）**  
    - 已加：`精品内容`三组滚动卡片（文章/项目/案例，按热度排序）。

17. 点赞收藏改成 AJAX 无刷新  
    - 状态：**已完成（第一批）**  
    - 已加：通用 `engagement-scripts` + 文章/项目/投稿三处按钮无刷新更新。

18. 每完成一项更新修复文档  
    - 状态：**进行中（当前文档已创建）**

## 待确认

- 站内通知的 `audience=user`（仅登录用户）是否需要单独支持？目前站内信天然是用户维度，`all` 等价“所有用户”。
