# OpenClaw 智信 - 收藏模块功能清单

> **模块：** 收藏模块  
> **页面路径：** `/dashboard/favorites`（路由名 `favorites.index`；清单旧版曾写 `/favorites`）  
> **功能数：** 4 个  
> **最后更新：** 2026-04-05

---

## 功能 1：收藏列表展示

| 项目 | 内容 |
|------|------|
| **功能描述** | 展示用户收藏的文章/项目/案例 |
| **页面位置** | 收藏列表页 |
| **涉及数据库** | 表：favorites, articles, projects, side_hustle_cases<br>字段：favorites.id, favorites.favoritable_type, favorites.favoritable_id, favorites.created_at, articles.title, projects.name |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**收藏卡片**</td><td>悬停</td><td>卡片上移 + 阴影加深</td><td>无</td></tr><tr><td>**收藏卡片**</td><td>点击</td><td>无</td><td>跳转对应详情页</td></tr></table> |
| **数据逻辑** | 1. 查询当前用户 `user_actions`（`type=favorite`，多态 `actionable`）<br>2. 关联文章 / 项目 / 案例<br>3. 分页 12 条/页 |
| **开发状态** | ✅ 已完成 |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 2：收藏分类筛选

| 项目 | 内容 |
|------|------|
| **功能描述** | 按类型筛选收藏内容 |
| **页面位置** | 收藏列表页顶部 |
| **涉及数据库** | 表：favorites<br>字段：favorites.favoritable_type |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**全部**</td><td>点击</td><td>高亮显示</td><td>显示所有收藏</td></tr><tr><td>**文章**</td><td>点击</td><td>高亮显示</td><td>筛选 favoritable_type='App\\Models\\Article'</td></tr><tr><td>**项目**</td><td>点击</td><td>高亮显示</td><td>筛选 favoritable_type='App\\Models\\Project'</td></tr><tr><td>**案例**</td><td>点击</td><td>高亮显示</td><td>筛选 favoritable_type='App\\Models\\SideHustleCase'</td></tr></table> |
| **数据逻辑** | 1. 根据 `type` 查询参数过滤 `actionable_type`<br>2. 高亮当前 Pill |
| **开发状态** | ✅ 已完成 |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 3：删除单个收藏

| 项目 | 内容 |
|------|------|
| **功能描述** | 删除单个收藏记录 |
| **页面位置** | 收藏卡片右上角（悬停显示） |
| **涉及数据库** | 表：favorites<br>字段：favorites.id |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**删除按钮**</td><td>悬停</td><td>显示删除图标</td><td>无</td></tr><tr><td>**删除按钮**</td><td>点击</td><td>显示确认弹窗</td><td>DELETE /favorites/{type}/{id}</td></tr><tr><td>**确认删除**</td><td>点击</td><td>显示"删除成功"</td><td>刷新列表</td></tr><tr><td>**取消删除**</td><td>点击</td><td>关闭弹窗</td><td>无</td></tr></table> |
| **API 调用** | `DELETE /dashboard/favorites/{userAction}`（`userAction` 为 `user_actions.id`，表单 `_method=DELETE`） |
| **数据逻辑** | 1. 校验归属当前用户且 `type=favorite`<br>2. 删除记录<br>3. 使用浏览器 `confirm()`，**非**独立模态框 |
| **开发状态** | ✅ 已完成 |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 4：批量删除收藏

| 项目 | 内容 |
|------|------|
| **功能描述** | 批量删除多个收藏记录 |
| **页面位置** | 收藏列表页底部 |
| **涉及数据库** | 表：favorites<br>字段：favorites.id |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**复选框**</td><td>勾选</td><td>显示已选数量</td><td>无</td></tr><tr><td>**批量删除**</td><td>点击</td><td>显示确认弹窗</td><td>POST /favorites/bulk-delete</td></tr><tr><td>**确认删除**</td><td>点击</td><td>显示"删除成功"</td><td>刷新列表</td></tr><tr><td>**取消删除**</td><td>点击</td><td>关闭弹窗</td><td>无</td></tr></table> |
| **API 调用** | `POST /dashboard/favorites/bulk-delete`，表单字段 `ids[]`（整数数组） |
| **数据逻辑** | 1. 校验均为当前用户收藏<br>2. 批量删除<br>3. `confirm()` 在 `form` 的 `onsubmit` |
| **开发状态** | ✅ 已完成 |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 📋 原型图对照（`docs/原型图/12-收藏列表页模块.md`）

> 核对代码：`_laravel_temp/resources/views/favorites/index.blade.php`（2026-04-05）

| 原型要素 | 当前实现 | 差异说明 |
|---------|---------|---------|
| 页面路径 | `/dashboard/favorites` | 原型写 `/favorites`，需以实际路由为准 |
| 筛选 | 全部 / 文章 / 项目 / 案例 Pill | **无**原型中的「标签▼」「搜索收藏」 |
| 卡片 | `oc-article-card` + 类型标签 + 标题 + 时间 | **无**封面图；删除为悬停显示「移除」按钮 |
| 批量 | 勾选 +「批量删除所选」 | 无独立「批量管理」模式 UI，逻辑等价 |

---

_文档版本：v4.0_  
_最后更新：2026-04-05_  
_维护者：OpenClaw 智信开发团队_
