# OpenClaw 智信 - 个人中心模块功能清单

> **模块：** 个人中心模块  
> **页面路径：** /dashboard  
> **功能数：** 6 个  
> **最后更新：** 2026-04-06

---

## 功能 1：头像上传

| 项目 | 内容 |
|------|------|
| **功能描述** | 上传用户头像 |
| **页面位置** | 个人中心 - 个人资料 |
| **涉及数据库** | 表：users<br>字段：users.id, users.avatar |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**头像预览**</td><td>悬停</td><td>显示"更换头像"按钮</td><td>无</td></tr><tr><td>**更换头像**</td><td>点击</td><td>打开文件选择器</td><td>无</td></tr><tr><td>**文件选择**</td><td>选择图片</td><td>显示图片预览</td><td>自动上传</td></tr><tr><td>**上传中**</td><td>自动</td><td>显示进度条</td><td>无</td></tr><tr><td>**上传成功**</td><td>自动</td><td>显示"上传成功"</td><td>刷新头像</td></tr><tr><td>**上传失败**</td><td>自动</td><td>显示错误提示</td><td>无</td></tr></table> |
| **API 调用** | `POST /profile/avatar`（Web，非独立 JSON API）<br>Content-Type: multipart/form-data，字段名 `avatar` |
| **数据逻辑** | 1. 验证用户已登录<br>2. 验证图片（`image`，≤2MB，`jpeg/jpg/png/gif/webp`）<br>3. 保存至 `storage/app/public/avatars/`，公开 URL 写入 `users.avatar`（需 `php artisan storage:link`）<br>4. 替换头像时删除旧 `public` 磁盘文件 |
| **开发状态** | 🟡 部分完成（有上传与预览；侧栏展示 **bio** 摘要；**无**上传进度条、**无**悬停蒙层式「更换」） |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 2：修改昵称

| 项目 | 内容 |
|------|------|
| **功能描述** | 修改用户昵称 |
| **页面位置** | 个人中心 - 个人资料 |
| **涉及数据库** | 表：users<br>字段：users.id, users.name |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**昵称输入框**</td><td>点击</td><td>显示输入光标</td><td>无</td></tr><tr><td>**昵称输入框**</td><td>输入</td><td>实时显示输入内容</td><td>无</td></tr><tr><td>**保存**</td><td>点击</td><td>显示"保存中..."</td><td>POST /profile/name</td></tr><tr><td>**保存成功**</td><td>自动</td><td>显示"保存成功"</td><td>无</td></tr></table> |
| **API 调用** | `POST /profile/name`，表单字段 `name` |
| **数据逻辑** | 1. 验证用户已登录<br>2. 验证昵称长度（2-50 字符）<br>3. 更新 users.name 字段 |
| **开发状态** | ✅ 已完成 |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 3：修改密码

| 项目 | 内容 |
|------|------|
| **功能描述** | 修改登录密码 |
| **页面位置** | 个人中心 - 账号安全 |
| **涉及数据库** | 表：users<br>字段：users.id, users.password |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**当前密码**</td><td>输入</td><td>显示密文</td><td>无</td></tr><tr><td>**新密码**</td><td>输入</td><td>显示密码强度</td><td><6 位：弱；6-10 位：中；>10 位：强</td></tr><tr><td>**确认新密码**</td><td>输入</td><td>实时对比</td><td>不一致时显示红色边框</td></tr><tr><td>**保存**</td><td>点击</td><td>显示"保存中..."</td><td>POST /profile/password</td></tr><tr><td>**保存成功**</td><td>自动</td><td>显示"密码已修改"</td><td>留在已登录态（与规格中「重新登录」可选差异）</td></tr></table> |
| **API 调用** | `POST /profile/password`，表单字段 `current_password`、`password`、`password_confirmation` |
| **数据逻辑** | 1. 验证用户已登录<br>2. 验证当前密码正确<br>3. 验证新密码长度≥6<br>4. 验证两次新密码一致<br>5. 加密新密码<br>6. 更新 users.password（成功后留在已登录态，非强制重新登录） |
| **开发状态** | ✅ 已完成 |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 4：修改邮箱

| 项目 | 内容 |
|------|------|
| **功能描述** | 修改登录邮箱 |
| **页面位置** | 个人中心 - 账号安全 |
| **涉及数据库** | 表：users<br>字段：users.id, users.email, users.email_verified_at |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**新邮箱**</td><td>输入</td><td>实时验证格式</td><td>格式错误时显示红色边框</td></tr><tr><td>**获取验证码**</td><td>点击</td><td>按钮变灰 + 倒计时</td><td>发送验证邮件</td></tr><tr><td>**邮箱验证码**</td><td>输入</td><td>自动聚焦</td><td>无</td></tr><tr><td>**保存**</td><td>点击</td><td>显示"保存中..."</td><td>PUT /profile/email</td></tr></table> |
| **API 调用** | `POST /profile/email/send-code`（JSON `email`）<br>`POST /profile/email`（表单：`email`、`verification_code`、`current_password`） |
| **数据逻辑** | 1. 验证用户已登录<br>2. 验证新邮箱格式<br>3. 验证新邮箱未被使用<br>4. 验证验证码正确<br>5. 更新 users.email 和 email_verified_at 字段 |
| **开发状态** | ✅ 已完成（`POST /profile/email/send-code` + `POST /profile/email`，新邮箱收验证码 + 当前密码） |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 5：订阅管理

| 项目 | 内容 |
|------|------|
| **功能描述** | 查看和管理订阅状态 |
| **页面位置** | 个人中心 - 订阅管理 |
| **涉及数据库** | 表：subscriptions, users<br>字段：subscriptions.plan, subscriptions.status, subscriptions.expires_at, users.role |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**当前套餐**</td><td>-</td><td>显示套餐信息</td><td>无</td></tr><tr><td>**到期时间**</td><td>-</td><td>显示剩余天数</td><td>无</td></tr><tr><td>**续费**</td><td>点击</td><td>无</td><td>跳转支付页</td></tr><tr><td>**升级套餐**</td><td>点击</td><td>无</td><td>跳转价格页</td></tr></table> |
| **数据逻辑** | 1. 查询当前用户的订阅记录<br>2. 显示 plan、status、expires_at<br>3. 计算剩余天数 |
| **开发状态** | 🟡 部分完成（侧栏展示身份 / 会员剩余天数 + 跳转「价格与套餐」「支付页」；**无**完整订阅订单列表与续费状态机） |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 功能 6：退出登录

| 项目 | 内容 |
|------|------|
| **功能描述** | 退出登录 |
| **页面位置** | 个人中心 - 顶部导航栏 |
| **涉及数据库** | 表：users<br>字段：users.remember_token |
| **交互详情** | <table><tr><th>元素</th><th>点击/悬停</th><th>反应</th><th>跳转/动作</th></tr><tr><td>**退出登录**</td><td>点击</td><td>直接提交表单（当前实现无确认弹窗）</td><td>POST /logout</td></tr><tr><td>**确认退出**</td><td>点击</td><td>清除 Session</td><td>跳转首页</td></tr><tr><td>**取消退出**</td><td>点击</td><td>关闭弹窗</td><td>无</td></tr></table> |
| **API 调用** | `POST /logout` |
| **数据逻辑** | 1. 清除 Session<br>2. 清除 remember_token<br>3. 跳转首页 |
| **开发状态** | ✅ 已完成（入口在顶栏用户下拉 `POST /logout`，非本页独立按钮；无二次确认弹窗） |
| **测试状态** | ⬜ 未测试 ⬜ 测试中 ⬜ 测试通过 |

---

## 📋 原型图对照（`docs/原型图/07-个人中心页模块.md`）

> 核对代码：`_laravel_temp/resources/views/dashboard.blade.php`（2026-04-05）

| 原型要素 | 当前实现 | 差异说明 |
|---------|---------|---------|
| 布局 | 左栏资料 + 订阅，右栏表单与快捷入口 | 与常见「侧栏 + 主内容」一致；具体栅格与原型线框可能略有不同 |
| 头像 | `POST /profile/avatar` + 选文件即提交 | 见功能 1「部分完成」 |
| 账号安全 | 昵称、密码、修改邮箱、个人简介（`bio`） | 简介无敏感词校验（增强项） |
| 订阅 | 链接至 `pricing`、`payments.confirm` | 非完整订阅管理表 |

---

_文档版本：v4.0_  
_最后更新：2026-04-05_  
_维护者：OpenClaw 智信开发团队_
