# 反代 / 负载均衡在更外层终止 HTTPS 时，会把 X-Forwarded-Proto 传到 Nginx。
# 若不转发给 PHP，Laravel 始终认为请求是 http，生成的 Livewire/Filament 脚本 URL 与浏览器 HTTPS 冲突（混合内容），登录会 405。
map $http_x_forwarded_proto $forwarded_proto {
    default $http_x_forwarded_proto;
    ''      $scheme;
}
